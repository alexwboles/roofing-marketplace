// Placeholder – later you can fetch aerial/satellite imagery here.
export async function fetchImageryForCoordinates(lat, lng) {
  return {
    url: "https://example.com/imagery.png",
    meta: { lat, lng }
  };
}
