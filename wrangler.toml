name = "roofing-marketplace2"
compatibility_date = "2024-01-01"

# This is the critical fix:
# It tells Cloudflare Pages to deploy the ROOT of your repo as the build output.
[pages]
pages_build_output_dir = "."

# Optional but recommended for consistency
[vars]
NODE_VERSION = "18"
